EJERCICIO 15

1. Entrar en la carpeta y visualizar el contenido de todos los ficheros:
cd restaurantes
cat *

2. Ordenar el archivo "restaurantes" por ciudad:
sort -t: +2 restaurantes

3. Mostrar cuántos restaurantes hay en Salamanca:
grep 'Salamanca' restaurantes | wc -l

4. Listado de restaurantes ordenado por id:
sort -t: +0 -n restaurantes

5. Guardar en "ficheroids" ordenado por id (una sola línea):
sort -t: +0 -n restaurantes > ficheroids

6. Guardar en "mejor" el restaurante con mayor puntuación:
sort -t: +3 -n -r restaurantes | head -1 > mejor

7. Listado con nombre y ciudad (solo esos dos campos):
cut -d: -f2,3 restaurantes

8. Generar fichero "ciudades" sin repetir:
cut -d: -f3 restaurantes | sort | uniq > ciudades


EJERCICIO 16

Listado de todos los restaurantes con todas las letras en mayúscula:
cat restaurantes | tr 'a-z' 'A-Z' (aquí cambia todo los campos)

cat restaurantes | cut -d: -f2 | tr 'a-z' 'A-Z' (aquí cambia todo los campos)

Generar fichero CIUDADES con nombres de ciudades en mayúscula (sin repetir):
cat restaurantes | cut -d':' -f3 | sort (opcional (va a ordenar)) | uniq | tr 'a-z' 'A-Z' > CIUDADES
(-d para indicar por donde separa y -f para extraer el campo que queremos)

Listado de todos los restaurantes sin que aparezca la letra "a":
cat restaurantes | tr -d 'a'


EJERCICIO 17

Muestra la localización de todos los archivos del sistema que comiencen por
“alsa”.
find /home/rodrigo -name "*alsa*"
Da igual la ubicación desde donde lo hagamos 
El * signifiaca que ignora lo que hay por delante y por detrás

Modifica la instrucción anterior para que no salgan mensajes de error.
????

Muestra todos los archivos de /usr que tengan extensión .png sin que salgan
mensajes de error.
find /usr -name "*.png"
* se pone para "ocultar lo que hay por delante"

Muestra todos los archivos de /usr que tengan la extensión .png y que ocupen
más de un MegaByte sin que salgan mensajes de error.
find /usr -name "*.png" -a -size +1M

Guarda en un fichero “imágenes.txt” el nombre del fichero y el tamaño de los
archivos de /usr que tengan la extensión .png y que ocupen más de un MegaByte
sin que salgan mensajes de error.
touch "imagenes.txt" | 



EJERCICIO 21

Prueba los comandos su y sudo.
sudo apt update (Actualiza la lista de paquetes del sistema, esencial en Debian/Ubuntu). 
(Si ponemos apt update solamente, nos mostrará errores y warnings. Si lo ponemos con con sudo, nos pedirá la contraseña y actualizara los paquetes)sudo 
sudo (super user do), hace que ciertos usuarios no root puedan ejecutar ciertos comandos que requieren privilegios, pero sin acceder al sistema como root. 


Busca como visualizar el contenido del archivo sudoers.
Habrá dos opciones:
	sudo visudo -c 	(-c (Check) --> lo que hace es verificar la sintaxis del archivo de configuración de sudo 				(normalmente /etc/sudoers) sin abrir el editor).
			(NO PERMITE VISUALIZAR EL CONTENIDO)

	sudo cat /etc/sudoers (o cd a /etc y luego sudo cat sudoers)

Busca la funcionalidad de visudo y pruébalo.
Se usa normalmente para:
	Validar cambios antes o después de editar sudoers
	Scripts o automatización para asegurarse de que la configuración es válida
	Diagnosticar problemas con sudo
sudo visudo -c sudoers

Modifica el archivo sudoers para añadir tu usuario con privilegios de root.
Se añadiría nombre usuario (Rodrigo) ALL(ALL:ALL) ALL 

Rodrigo ALL=(ALL:ALL) ALL

Primer ALL: desde cualquier host
(ALL:ALL): puede ejecutar como cualquier usuario y grupo
Ultimo ALL: puede ejecutar cualquier comando

Comprueba si cambia o no el prompt cuando ejecutas el comando sudo su. 
Si cambia. Pasa de: rodrigo@rodrigo-VirtualBox a root@rodrigo-VirtualBox cambia también el símbolo del dólar $ a #


EJERCICIO 22



